# Adding all the necessary ros sourcing
echo "" >> ~/.bashrc
echo "## ROS2" >> ~/.bashrc
echo "source /home/ros2/ros2_ws/install/setup.bash" >> ~/.bashrc

echo "" >> /root/.bashrc
echo "## ROS2" >> /root/.bashrc
echo "source /home/ros2/ros2_ws/install/setup.bash" >> /root/.bashrc

echo "session required pam_limits.so" | sudo tee --append /etc/pam.d/common-session > /dev/null