syntax = "proto3";

package ProtocolBuffers;

message DynamicalSystemMsg 
{
	string type = 1;
	repeated double gains = 2;
	repeated string parameter_names = 3;
	repeated double parameter_values = 4;
}